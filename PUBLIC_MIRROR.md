# Public mirror

This repository is the public mirror of a private development repository, built by `pnpm mirror:build` and carrying no prior history. Commits here are made under the project's neutral identity (`Va de Vi <noreply@example.invalid>`); the pre-push hook refuses any other, so a maintainer's address never reaches the public record.
